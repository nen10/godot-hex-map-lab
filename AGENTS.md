# AGENTS.md

必要な作業を発見・承認する。
方針はREADME.mdおよび`docs/plan/`以下を確認する。
意味のある機能には簡単なテストを作成する。意味のある機能とは、`docs/plan/`以下で計画されている機能である。
実装の有無・内容はテストベースで確認する。

  - 実装済み各テストファイルは `docs/TEST.md` Test path セクションを参照

`docs/plan/`以下で計画されている機能がTESTによって確認されている場合、`docs/complete_on_test/`以下にdocument移動し、未実装planから完了扱いに変更したことがわかるようにする。

fallbackは実装上の一時的な状態として扱い、ドキュメント記載は仕様として固定化されず、方針の根拠としない。ドキュメントに存在するfallback処理の記載は実装の根拠ではなく一時的な対応結果の報告である。

実装計画を記録する場合、入出力のインターフェースやリソースとなるファイルのスキームを明確にする。テストの概要はそれに従った前提で簡潔にdocumentationする。
作業を行わないことをドキュメントに記述してはいけない。順次行うための作業範囲をテスト計画の形で記述することは構わない。
順序を問わない着手可能な課題については最も大きな課題から取り組み、細かい課題は別のエージェントに投げてよい。実現できなかった範囲があれば需要されているテストケース候補としてまとめる。
実装済みのplan内容について、細かいものはtestとして簡潔に表現する。
ユーザーはreviewを行い、完了の承認と必要に応じてplan以下に要望ファイルを作成する。
ユーザーが要望するか、完了を承認した機能に対してのみ、manualを作成する。

作業の一貫性ではなく目的の一貫性をとる。
作業の効率化のために簡単にできることがあれば記録したりtoolを作成してよい。

  - Note: テスト実行は `tools/test.sh` を使用してよい。詳細は `docs/TEST.md` の 実行 セクションを参照
  - Note: minimal に godot を headless 実行する場合 ` --log-file .godot_user/<purpose>.log ` を付けることでクラッシュを回避します。
