# DOC-90 Workspace UI Manual Update Policy

Date: 2026-06-08

## Decisions

- Update user-facing manuals to match the current `Resources` tab and selected `HexTileMap` auto-link workflow.
- Explain sample learning as optional onboarding and duplication, not an execution fallback.
- Explain `Generate` output target choices as `Preview only` and `Apply to selected Document`.
- Keep the test update to documentation coverage only.

## Non-goals

- Do not change editor UI behavior in this task.
- Do not add new analog tests.
- Do not reintroduce editable path text, numeric fallback, or sample-only completion language.
