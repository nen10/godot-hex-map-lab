# PKG-70 Policy

## Requirement

The package/sample learning contract must verify bundled sample availability and opt-in visibility:

- package manifest includes sample catalog, tile texture, and scene
- runtime/editor loading can access sample resources
- sample mode OFF hides main selector fallback
- sample mode ON exposes learning candidates

## Boundary

Project asset clean install coverage belongs to `PKG-71`.
