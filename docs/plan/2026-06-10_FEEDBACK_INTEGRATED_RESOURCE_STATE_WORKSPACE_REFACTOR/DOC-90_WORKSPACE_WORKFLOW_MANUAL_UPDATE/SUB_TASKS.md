# DOC-90 Workspace Workflow Manual Update Sub Tasks

## Task Resolution

| candidate | decision | reason |
|---|---|---|
| Update editor/manual workflow order around selected `HexTileMap` -> Resources -> Generate -> Paint -> Catalog -> Validate -> QA -> Export. | Adopt | The roadmap acceptance asks for this visible workspace route, even when Catalog setup can happen before Paint in a new project. |
| Add source badge meanings for `Node`, `Project`, `Document Dependency`, `Manual Override`, `Sample Learning`, and `Missing`. | Adopt | Badge semantics are now a primary resource ownership contract and need user-facing documentation. |
| Keep sample learning as its own chapter/path. | Adopt | Samples are onboarding assets and must not read like production fallbacks. |
| Add or rewrite analog tests for manual coverage. | Reject | CLEAN UI work explicitly avoids new analog tests unless requested. |
| Expand README with a short orientation note if the manual changes need a landing pointer. | Adopt | README should point users toward the same ownership and source-badge language without duplicating the manual. |

## Scheduled Task

No follow-up task is scheduled from this slice. This task is a documentation alignment pass and does not expose unresolved implementation work.
