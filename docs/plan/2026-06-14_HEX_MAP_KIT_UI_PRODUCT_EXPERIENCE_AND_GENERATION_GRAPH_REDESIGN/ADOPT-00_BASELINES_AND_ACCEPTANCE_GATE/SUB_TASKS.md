# ADOPT-00 SUB_TASKS

Complexity class: **C2**（process rule の1 slice。state 遷移なし、fallback/mirror なし、UI なし。）

Task resolution:
- 改訂 Roadmap / baseline 2点を source-of-truth として固定し、**二層 DoD（structural + experiential）acceptance gate** を process docs と self-review template に実装する。QA/Validate の park を process 規則化する。

Scheduled Task Audit: none（このtaskは scheduled task を生成しない）。

fallback / mirror 有無: none。

Sub-tasks:
1. self-review template に experiential DoD セクションを追加。
2. queue operation rules に two-layer DoD gate と QA park 規則を追加。
3. planning policy の review checklist に experiential DoD / baseline 整合を追加。
4. queue を更新（ADOPT-00 → COMPLETE、依存 task を READY 化、pointer 更新）。
5. proof log / self-review を記録。
