# GAME-05 Implementation Plan

Date: 2026-06-07

## Steps

1. Mark `GAME-05` as `RUNNING` in the queue.
2. Add `examples/basic_runtime/runtime_query_sample.gd`.
3. Add a short `examples/basic_runtime/README.md`.
4. Extend `tests/test_debug_scenes.gd` to save a v2 document fixture and query it through the sample.
5. Update scripting/test docs.
6. Run `./tools/test.sh`, self-review, repair if needed, update queue proof, and commit.
