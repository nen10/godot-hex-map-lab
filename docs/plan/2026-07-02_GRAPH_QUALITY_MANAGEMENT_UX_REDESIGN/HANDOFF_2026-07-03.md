# GQM track ハンドオフ（2026-07-03 中断時点）

状態: **使用料都合による中断**。orchestrator（Fable）の自律実行を停止。バグ対応はユーザーが引き取り。
branch: `mainline/generate-perf-clean`（HEAD = 54c1e3f + 本 handoff commit。全て commit 済み・作業ツリー clean）

## 1. 完了済み（gate ACCEPT → merge → 統合 test.sh green）

| task | 内容 | 視覚検証 |
|---|---|---|
| GQM-01 | 統合4ノード engine・adaptation 全域関数・legacy 正規化・`would_create_cycle` | —（headless） |
| GQM-02 | 資産二層 service（bundled/project）・`HexItemPoolResource`・`from_preset` | — |
| GQM-03 | 統合4型の宣言的 param schema（visible_when/label_by_mode/derived_default/affects/asset_kind）+ default 常時有効 | — |
| GQM-10 | 無型 canvas（type 0・一色）・adaptation 行・循環拒否・統合4型 Add 行 | PASS（`GQM-10/basic_form_consolidated_canvas.png`） |
| GQM-11 | schema 駆動 inspector・titlebar 資産 chip・morph・selection 表示 | PASS（`GQM-11/*.png` 3枚） |
| GQM-16 | 資産 reference の実行時解決（3 asset path key）・runtime parity・edge adaptation round-trip | — |
| GQM-15 | legacy 読込正規化（明示通知付き）・Simple preset の統合2ノード化 | — |
| GQM-12 | Result 行の並べ替え・write policy・行 Promote・unused 明示 | PASS（`GQM-12/result_stack_rows.png`） |
| GQM-18 | chip の資産操作 menu 一本化・path 行非表示・未接続/そのまま分離・titlebar 幅 | PASS（GQM-13 と合同） |
| GQM-13 | header 一掃・Template ▾（bundled `basic.tres`=基本形 / `simple.tres`）・graph Save as/Load・Generate 一本化 | PASS（`GQM-13-18/swept_header_basic_template.png`） |

視覚検証キャプチャ: `.godot_user/visual-verification/GQM-*/`。probe: `tools/probe_gqm10_canvas_visual.gd` / `probe_gqm11_ui_visual.gd` / `probe_gqm12_result_visual.gd` / `probe_gqm13_screen_visual.gd` / `probe_gqm17_windows.gd`（すべて commit 済み）。

orchestrator 修理 2 件（proof log 記載済み）: (1) GQM-11×16 並行 merge の意味的衝突（chip preset 名解決を `distribution_id` 宣言に固定）、(2) 読込時ノード配置のトポロジ段組みレイアウト（`_restore_layout_depths`、列 pitch 540）。

## 2. 未完了

### GQM-14_RESULT_RESOURCE_SAVE_SWITCH（READY・未着手）
- Codex ワーカーが **usage limit（10:48 AM 回復）** で起動失敗。worktree `../wt-gqm14`（branch `agent/cx-GQM-14`、54c1e3f 起点）は無傷で待機中。
- 10:50 の自動再投入 job は**停止済み**（credit 消費防止）。再開はこの 1 コマンド:
  ```sh
  codex exec -s workspace-write -C ../wt-gqm14 -o /tmp/codex_gqm14.md - \
    < docs/plan/2026-07-02_GRAPH_QUALITY_MANAGEMENT_UX_REDESIGN/GQM-14_CONTRACT.md
  ```
  完了後: `python3 tools/verify_task.py --task GQM-14_RESULT_RESOURCE_SAVE_SWITCH --head agent/cx-GQM-14 --queue docs/plan/2026-07-02_GRAPH_QUALITY_MANAGEMENT_UX_REDESIGN/IMPLEMENTATION_QUEUE.md` → ACCEPT なら merge。

### GQM-17_CRITERIA_WINDOW_DOD（検証中・中間所見あり）
DoD =「各パラメータが不快感なく UI に反映」。probe `tools/probe_gqm17_windows.gd` の 4 キャプチャ（`.godot_user/visual-verification/GQM-17/`）まで実施。

**中間所見（バグ候補・ユーザー引き取り分）**:
1. **Adjacency Rules window のパターン hex が全白**（渡した directions が黒トグル表示されない・成分ラベルが `{}`）。ただし **probe 側の key 形式ミスの可能性が高い**: `HexVector.key()` は `"%d,%d,%d" % [q, s, r]`（**q, s, r 順**）。probe は q,r,s 順のつもりで `"1,-1,0"` 等を渡した。**正しい key 順で再検証してから**製品バグか判定すること。
2. Adjacency window の **Rule set name field が狭く truncate**（"Rule se"）。
3. Adjacency window の **layout が 1200x860 で崩れ気味**（Preset dropdown が全幅・dialog サイズ計算の再確認。900x760 の旧 probe サイズでは正常だった可能性）。
4. Markov window の weight spin が `1.0` 表示（0..8 の整数 scale に対し float 表示。cosmetic）。
5. Markov window は §4 文法（Preset dropdown + Load/Save as/Duplicate/Inline）が実装済みで概ね良好。item pool の limited 行（count 表示）も良好。chip の `pool: inline` は method 切替（limited→weighted）を跨いで安定。

GQM-17 完了条件: 上記所見の解消確認 + DoD checklist を PROOF_LOG に記録 → queue 行 COMPLETE。

## 3. 運用メモ（再開時）

- **Pipeline**: worktree 分離 → 契約（acceptance/depth=integrated/scope/禁止/self-review 完全 task id 命名）→ codex exec → `verify_task.py --queue <GQM queue>` → merge → 統合 `./tools/test.sh` → UI task は実描画キャプチャ検証。
- ワーカーが commit 不能な場合（sandbox が `.git/worktrees/*` に書けない）: orchestrator が代行 commit（1 commit・`feat(hex_map_kit): GQM-XX ...`）。
- queue/PROOF_LOG の並行 merge conflict は「自 task 行のみ採用・proof は両方連結」で解消。
- merge 済み worktree（wt-gqm01/02/03/10/11/12/13/15/16/18）は `git worktree remove` で掃除可。**wt-gqm14 は残すこと**。
- 全体設計の一次資料: `DESIGN_DIALOGUE.md`（round 1-4）/ `RESOURCE_MODEL.md` / `DEPENDENCY_UX_PROPOSALS.md`（統合4ノード・adaptation・翻訳表）。
