# GQM-17 Criteria Window DoD Implementation Plan

## Scope

- Correct the GQM-17 visual probe so it does not manufacture Adjacency failures through the wrong direction-key order.
- Extend the probe to verify Markov weight SpinBox ranges and capture a visible `8.0` value.
- Repair only confirmed product UI discomfort in criteria windows.
- Update completion proof and queue status.

## Target Files

- `tools/probe_gqm17_windows.gd`
- `addons/hex_map_kit/editor/hex_map_build_node_inspector.gd`
- `docs/plan/2026-07-02_GRAPH_QUALITY_MANAGEMENT_UX_REDESIGN/IMPLEMENTATION_QUEUE.md`
- `docs/plan/2026-07-02_GRAPH_QUALITY_MANAGEMENT_UX_REDESIGN/PROOF_LOG.md`
- `docs/review/autopilot/GQM-17_CRITERIA_WINDOW_DOD_SELF_REVIEW_2026-07-03.md`

## Planned Implementation Steps

1. Update `probe_gqm17_windows.gd` to build Adjacency direction keys from `HexVector.directions()`.
2. Add Markov SpinBox range logging and set one weight to `8.0` before the proof capture.
3. Run the non-headless visual probe and inspect the generated captures.
4. If corrected proof still shows product-side discomfort, repair only that confirmed UI issue.
5. Run `git diff --check`, the visual probe, and `./tools/test.sh`.
6. Record self-review, proof log, and queue completion.

## Dependency / Test Matrix

| dependency / area | risk | proof / test |
|---|---|---|
| `HexVector.key()` order | q,s,r vs q,r,s confusion | Probe prints keys generated by `HexVector.directions()`. |
| Markov weight range | Default `1.0` mistaken for max `1.0` | Probe logs every SpinBox min/max/step and captures `8.0`. |
| Adjacency window ergonomics | Name field or pattern list clipped at first open | Rendered `adjacency_window_consolidated.png`. |
| Existing editor regressions | Small UI polish could affect tests | `./tools/test.sh` and UI metric report. |

## Planned Completion Criteria

- Visual captures exist for Markov, Markov `8.0`, Adjacency, and Item Pool states.
- Markov range proof shows `0.0..8.0` without changing product semantics.
- Adjacency pattern toggles reflect corrected direction keys.
- Adjacency name field and initial dialog layout are usable at the verification viewport.
- `./tools/test.sh` exits `0`.
